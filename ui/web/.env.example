# URL of the goosed backend server
# Start goosed with: goosed agent
# Then set this to its URL (check goosed output for the port)
VITE_GOOSE_API_HOST=http://localhost:3000

# Optional: secret key for API authentication
# Only needed if goosed is configured to require it
VITE_GOOSE_SECRET_KEY=
